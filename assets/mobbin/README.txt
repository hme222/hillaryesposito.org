Drop screenshots in this folder using these exact filenames:

  kikoff.png       — Kikoff phone screen (used on case study)
  polymarket.png   — Polymarket phone screen (used on case study)
  discover.png     — Discover phone screen (used on case study)
  mobbin-hero.png  — composite of all three phones (used on Home projects card)

Recommended size: ~9:19.5 portrait crop, 1080px wide or larger.
Until the files are added, each phone shows a gradient placeholder
with the app name — the page works fine without images.
