# Allow all web crawlers access to everything except admin and private folders

User-agent: *
# Tailored role pages and private labs are direct-link only and carry noindex at
# runtime — but that meta tag is applied by JavaScript, so non-rendering
# crawlers never see it. Disallow them here too.
Disallow: /curated/
Disallow: /lab/

# Sitemap location (optional but recommended for SEO)
Sitemap: https://hillaryesposito.org/sitemap.xml
